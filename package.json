{
  "name": "gatsby-github-kinshasa",
  "description": "Gatsby demo with GitHub GraphQL (Kinshasa).",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "gatsby": "^1.9.247",
    "gatsby-link": "^1.6.40",
    "gatsby-plugin-react-helmet": "^2.0.10",
    "react-helmet": "^5.2.0"
  },
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gatsby-source-github": "^0.0.2",
    "prettier": "^1.12.0"
  }
}
